---
- name: DB_Deploy
  hosts: 172.16.14.142

  tasks:
    - name: call python
      shell:
       cmd: |
         . ~/.bash_profile;
         echo --ticket_no {{ ticket_no }} --projname {{ projname }} --Jenkins_Base_Id_Name {{ Jenkins_Base_Id_Name }} --env {{ env }}